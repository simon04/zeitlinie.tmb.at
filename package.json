{
  "name": "zeitlinie.tmb.at",
  "private": true,
  "author": "Simon Legner <Simon.Legner@gmail.com>",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "esbuild --bundle app.ts --outdir=dist/ --loader:.geojson=json --loader:.png=file --target=es6 --minify --sourcemap",
    "test": "echo No tests specified"
  },
  "dependencies": {
    "leaflet": "^1.9.3",
    "leaflet-providers": "^1.13.0",
    "leaflet.timeline": "^1.4.3"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.1",
    "@types/leaflet-providers": "^1.2.1",
    "esbuild": "^0.7.21"
  }
}
