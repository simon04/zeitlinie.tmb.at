{
  "name": "zeitlinie.tmb.at",
  "private": true,
  "author": "Simon Legner <Simon.Legner@gmail.com>",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "esbuild --bundle app.ts --outdir=dist/ --target=es6 --minify --sourcemap --external:images/layers.png --external:images/layers-2x.png --external:images/marker-icon.png",
    "test": "Echo no tests specified"
  },
  "dependencies": {
    "leaflet": "^1.7.1",
    "leaflet-providers": "^1.10.2",
    "leaflet.timeline": "^1.4.3"
  },
  "devDependencies": {
    "@types/leaflet": "^1.5.19",
    "@types/leaflet-providers": "^1.2.0"
  }
}
